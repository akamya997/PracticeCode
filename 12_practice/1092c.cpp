#include<bits/stdc++.h>
using namespace std;
vector<string> cons;
int ans[305];
int len[305];
int main()
{
	std::ios::sync_with_stdio(false);
	cin.tie(NULL);
	int n;
	memset(ans,0,sizeof(ans));
	cons.clear();
	cin>>n;
	string temp;
	for(int i=0;i<2*n-2;i++)
	{
		cin>>temp;
		cons.push_back(temp);
	}


	
}
